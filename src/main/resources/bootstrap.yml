spring:
  application:
    name: MyFirstConsulApp
#  cloud:
#    config:
#      uri: http://localhost:8500
#spring:
  cloud:
    consul:
      host: localhost
      port: 8500
      config:
        enabled: true
        format: yaml
        prefix: config
        data-key: keys
        defaultContext: apps
        profileSeparator: '::'